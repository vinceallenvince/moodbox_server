extends layout

block content
    div(class="jumbotron")
      div(class="container")
        h1= pageTitle
          p
          p
            a(href=authorizeURL, class="btn btn-primary") Please authorize

